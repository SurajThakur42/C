#include<stdio.h>

int main(){
    int a; /*      1. First character must be an alphabet or underscore (_) 
                   2. No commas, blanks are allowed. 
                   3. No special symbol other than (_) allowed. 
                   4. Variable names are case sensitive. */
    a = 6;
    printf("The output of this program is %d",a);
    return 0;
}
